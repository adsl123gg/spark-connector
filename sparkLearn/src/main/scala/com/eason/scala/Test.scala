package com.eason.scala

object Test {
  def main(args: Array[String]): Unit = {
//    val json = "{\"symbol\":\"AAPL\",\"trade\":\"SELL\",\"price\":20.0,\"quantity\":12,\"id\":123}"
//    NewStockTradeHqc.fromJsonAsBytes(byteArray)
    /*val data = List(1,3,4,5,6,9,3,2)
    data.foldLeft(
      (Nil: Seq[Int])
    ) { (z, x) =>
        println(x)
        val records = z
        if (records.sum > 10) {
          println(records)
          x +: Nil
        } else {
          records :+ x
        }
    } match {
      case lst => println(lst + lst.length.toString)
      case _ => println("other")
    }*/

  }
}
